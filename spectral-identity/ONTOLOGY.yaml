# ONTOLOGY.yaml module - 2026n
#
# Per-paper ontology module per [internal path removed]
# §"ONTOLOGY.yaml schema (v0.1)". owns = terms THIS paper introduces (unique
# authority); imports = terms owned by other modules it uses (reference only).
# Linked into the substrate `terms` graph by build_ontology.py; rendered per-paper
# by render_paper_glossary.py. Bootstrapped 2026-06-13 from per-paper term
# extraction; definitions paraphrase each paper's own definitional text.

paper_key: 2026n
schema_version: 0.1

owns:
  - term_key: spectral-identity
    label: "Spectral Identity"
    canonical_form: "spectral identity"
    definition: "The formalization of Aaker's brand identity as a parametrized eight-dimensional perceptual structure with observer-specific spectral weights, converting judgment-dependent identity decisions into testable, computable operations."
    first_section: "Title / Abstract"
  - term_key: spectral-weight
    label: "Spectral Weight"
    canonical_form: "spectral weight"
    definition: "A cohort's per-dimension contribution coefficient across the eight dimensions; core identity elements correspond to highest-weight dimensions and extended elements to moderate-weight dimensions, making the core/extended distinction empirically discoverable."
    first_section: "4.2 Core and Extended Identity as Spectral Weights"

imports:
  - {term_key: spectral-brand-theory}
  - {term_key: spectral-dimensions}
  - {term_key: observer-spectral-profile}
  - {term_key: cohort}
  - {term_key: perception-cloud}
  - {term_key: brand-conviction}

refines: []

