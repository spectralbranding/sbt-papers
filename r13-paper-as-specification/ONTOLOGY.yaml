# ONTOLOGY.yaml module - 2026t
#
# Per-paper ontology module per [internal path removed]
# §"ONTOLOGY.yaml schema (v0.1)". owns = terms THIS paper introduces (unique
# authority); imports = terms owned by other modules it uses (reference only).
# Linked into the substrate `terms` graph by build_ontology.py; rendered per-paper
# by render_paper_glossary.py. Bootstrapped 2026-06-13 from per-paper term
# extraction; definitions paraphrase each paper's own definitional text.

paper_key: 2026t
schema_version: 0.1

owns:
  - term_key: paper-spec
    label: "Paper Spec"
    canonical_form: "Paper Spec"
    definition: "An open, human-writable, machine-readable YAML standard that captures a paper's claims, methodology, acceptance criteria, results, dependencies, contradictions, and limitations in a companion paper.yaml file without replacing the paper itself."
    first_section: "1. Introduction"
  - term_key: verification-as-specification-crisis
    label: "Verification crisis as specification crisis"
    canonical_form: "the verification crisis is a specification crisis"
    definition: "The diagnosis that science's generation-verification gap is fundamentally a lack of structured specification, following the historical generation-specification cycle pattern where each generation breakthrough was resolved by a specification breakthrough rather than faster consumption."
    first_section: "1. Introduction"

imports:
  - {term_key: spectral-brand-theory}

refines: []

