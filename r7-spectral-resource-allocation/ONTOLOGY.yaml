# ONTOLOGY.yaml module - 2026k
#
# Per-paper ontology module per [internal path removed]
# §"ONTOLOGY.yaml schema (v0.1)". owns = terms THIS paper introduces (unique
# authority); imports = terms owned by other modules it uses (reference only).
# Linked into the substrate `terms` graph by build_ontology.py; rendered per-paper
# by render_paper_glossary.py. Bootstrapped 2026-06-13 from per-paper term
# extraction; definitions paraphrase each paper's own definitional text.

paper_key: 2026k
schema_version: 0.1

owns:
  - term_key: alignment-gap
    label: "Alignment Gap"
    canonical_form: "alignment gap"
    definition: "The economic value loss incurred when a brand's signal portfolio is optimized for the founder's spectral weight profile rather than the target cohort's, bounded below by the Fisher-Rao distance between the two weight profiles on the probability simplex."
    first_section: "The Alignment Gap — Definition"
  - term_key: concentration-premium
    label: "Concentration Premium"
    canonical_form: "concentration premium"
    definition: "The result that cohorts with more concentrated weight profiles (higher Herfindahl index) yield strictly higher optimal perceived value, making niche cohorts inherently more valuable to serve than diffuse mass-market cohorts at equal cost."
    first_section: "Optimal Value and the Value of Information"

imports:
  - {term_key: spectral-dimensions}
  - {term_key: observer-spectral-profile}
  - {term_key: cohort}
  - {term_key: structural-absence}
  - {term_key: perception-cloud}

refines: []

