anthropic>=0.42.0
openai>=1.60.0
google-genai>=1.0.0
numpy>=2.0.0
scipy>=1.14.0
pyyaml>=6.0
# Note: all API calls use temperature=0.7 (not default) to measure cross-model variance.
# Local models (qwen3:30b, gemma4:latest) require Ollama running at localhost:11434.
# Install: uv pip install -r requirements.txt
